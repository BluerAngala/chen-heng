# Build artifacts (bundled for OpenClacky publish only — never committed)
dist/
profile-card.html

# Local secrets (publish tokens, etc.)
token.json
*.token
.env
.env.local

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Generated by `gh repo create` on push
.README.md.bak
